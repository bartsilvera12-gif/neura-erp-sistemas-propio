import ConciliacionClient from "./ConciliacionClient";

/** Cobranzas → Conciliación bancaria (aprobar/rechazar transferencias). */
export default function Page() {
  return <ConciliacionClient />;
}
